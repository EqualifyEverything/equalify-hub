{
  "name": "equalify-hub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx @tailwindcss/cli -i ./styles/input.css -o ./styles/tailwind.css --minify && node --env-file-if-exists=.env --import tsx --loader ./dev/css-loader.mjs ./dev.tsx",
    "build": "npx @tailwindcss/cli -i ./styles/input.css -o ./styles/tailwind.css --minify && esbuild ./index.ts --bundle --platform=node --outdir=dist --external:@aws-sdk --jsx=automatic --jsx-import-source=hono/jsx --loader:.tsx=tsx --loader:.css=text --minify && cd dist && zip -r lambda.zip * > /dev/null && aws --profile equalifyuic lambda update-function-code --function-name equalifyuic-hub --zip-file \"fileb://lambda.zip\" > /dev/null && rm -rf lambda.zip",
    "sso": "aws sso login --profile equalifyuic"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.12.9"
  },
  "imports": {
    "#src/*": "./*"
  },
  "type": "module",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1018.0",
    "@aws-sdk/lib-dynamodb": "^3.1018.0",
    "@hono/node-server": "^1.19.11",
    "esbuild": "^0.27.4",
    "tailwindcss": "^4.2.2",
    "tsx": "^4.21.0"
  }
}
